{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd \"backend authentication\" && npm install"
  },
  "deploy": {
    "startCommand": "cd \"backend authentication\" && npm start",
    "restartPolicyType": "ON_FAILURE"
  }
}
